---
interface Props {
  href: string;
  color?: "primary" | "black" | "white";
  lg?: boolean;
  class?: string;
  [key: string]: any; // Allow passing extra HTML anchor attributes
}

const {
  href,
  color = "primary",
  lg = false,
  class: className,
  ...rest
} = Astro.props;

const colorClasses = {
  primary: "text-primary",
  black: "text-black ",
  white: "text-white ",
};

const activeColorClass = colorClasses[color] || colorClasses.primary;
---

<a
  href={href}
  class:list={[
    "link-more inline-flex items-center gap-1 uppercase transition-colors duration-200",
    lg ? "link-more--large" : "text-btn",
    activeColorClass,
    className,
  ]}
  {...rest}
>
  <slot>VER MÁS +</slot>
</a>

<style>
  .link-more {
    position: relative;
    padding-bottom: 4px;
  }

  .link-more.link-more--large {
    font-size: var(--fs-p-large);
    font-weight: 600;
    line-height: var(--lh-btn);
    letter-spacing: var(--ls-btn);
    padding-bottom: 6px;
  }

  .link-more::after {
    content: "";
    position: absolute;
    left: 0;
    bottom: 0;
    width: 100%;
    height: 2px;
    background-color: currentColor;
    transform: scaleX(0);
    transform-origin: right center;
    /* Delay on leave keeps the collapse from feeling abrupt on fast passes */
    transition: transform 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94) 0.25s;
  }

  .link-more:hover::after {
    transform: scaleX(1);
    transform-origin: left center;
    /* Delay on enter so quick fly-overs never trigger the underline */
    transition: transform 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94) 0.2s;
  }

  @media (prefers-reduced-motion: reduce) {
    .link-more::after {
      transition: none;
    }
  }
</style>
